# 🔒 الأمان — Security Policy

**شركة نجوم دلتا للتجارة (Delta Stars Trading Co.)**

## الإبلاغ عن ثغرة أمنية (Vulnerability Disclosure)

نرحب بالتقارير المسؤولة عن أي ثغرة أمنية في المتجر أو التطبيقات (الويب، Android، iOS).

- **البريد**: `INFO@DELTASTARS-KSA.COM` (يرجى استخدام التشفير عند الإمكان)
- **العنوان الرسمي**: `https://deltastars.store/.well-known/security.txt`
- **يرجى تضمين**: الخطوات القابلة للتكرار، الإصدار المتأثر، والأثر المحتمل.

**ما نرجوه:**
- لا تستغل الثغرة بعد اكتشافها، وأرسل التقرير قبل نشر أي تفاصيل علنية.
- لا تختبر على الحسابات الحقيقية أو بيانات العملاء.
- انتظر الرد (عادة خلال 48 ساعة) والجدول الزمني للإصلاح (عادة 14-30 يوماً).

## الممارسات الأمنية المطبقة

| الطبقة | التنفيذ |
|:---|:---|
| تشفير النقل | HTTPS إلزامي + HSTS (max-age=63072000; preload) + TLS 1.2/1.3 |
| رؤوس الأمان | CSP صارمة، X-Frame-Options: DENY، nosniff، Referrer-Policy، Permissions-Policy |
| المصادقة | OTP عبر الهاتف (Authentica)، JWT، بلا كلمات مرور للتسجيل، تغيير إجباري للكلمات الأولية |
| التحقق الحيوي | WebAuthn (ويب) + BiometricPrompt/LocalAuthentication أصلي (تطبيق) |
| تخزين الأسرار | خزنة SHA-256 موزعة الملح محلياً؛ مفاتيح API في لوحة Freebuff/CI فقط |
| الدفع | بوابة Moyasar بحقول معزولة — لا تخزين لبيانات البطاقات (PCI DSS) |
| الحماية من الهجمات | Rate limiting على OTP، حماية SQL Injection/XSS/CSRF، WAF على مستوى الكود |
| التحديثات | Dependabot أسبوعياً + UpdateManager يبث التحديثات تلقائياً |

## النطاق

- `https://deltastars.store` وتطبيقاته الأصلية (`com.deltastars.store`).
- خارج النطاق: الدومينات التابعة لأطراف خارجية (Moyasar، Supabase، Firebase، Google) — أبلغ مزودها مباشرة.

## 📋 تقرير تدقيق التبعيات (npm audit) — القرارات الموثقة

تم تشغيل `npm audit fix` بمراجعة آمنة (بدون `--force`)، وترقية يدوية آمنة لـ `jspdf@^4.2.1` و`vite@^6.4.3` (ضمن نفس الإصدار الرئيسي)، ونقل أدوات التوليد إلى `devDependencies`.

| الحزمة | الحالة | القرار |
|:---|:---|:---|
| `react-router` / `react-router-dom@7.18.2` | إشعار عالي (GHSA-qwww-vcr4-c8h2) | **غير قابل للتطبيق**: الاستشهاد يخص فقط وضع RSC غير المستقر — تطبيقنا SPA كلاسيكي (BrowserRouter) بلا أي استخدام لـ RSC. الإصلاح المقترح من npm (تخفيض إلى 7.11.0) هو تراجع خطير، وترقية v8 تغيير كاسر — لذا أبقينا الأحدث 7.18.2 ونراقبه عبر Dependabot. |
| `@capacitor/assets@3.0.5` وسلسلته (`sharp@0.32.6`, `tar@6.2.1`, `minimatch@3.0.5`, `uuid`) | إشعارات متوسطة/عالية/حرجة | **أدوات بناء فقط**: كلها متداخلة داخل مولّد الأيقونات `@capacitor/assets` (أحدث إصدار متاح 3.0.5) وتعمل في وقت البناء فقط ولا تُشحن في التطبيق إطلاقاً. فرض ترقيات قسرية (`overrides`) قد يكسر `build:assets`. نراقب إصدارات Capacitor عبر Dependabot. |

> **الخلاصة:** سطح التشغيل (production runtime) نظيف من الثغرات القابلة للاستغلال. كل ما تبقّى هو أدوات بناء dev-only أو استشهاد RSC غير مطبّق، وتتم مراقبته آلياً أسبوعياً عبر Dependabot.

---

*© 2026 شركة نجوم دلتا للتجارة — جميع الحقوق محفوظة.*
